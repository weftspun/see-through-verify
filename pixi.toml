[project]
name = "see-through-verify-tools"
version = "0.1.0"
description = "Model weight conversion tools for see-through-verify"
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tasks]
download-models = "python tools/download-models.py"

[dependencies]
python = ">=3.12"
pyarrow = ">=14.0.0"
requests = ">=2.31.0"