[project]
name = "see-through-verify-tools"
version = "0.1.0"
description = "Model weight download tools for see-through-verify"
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tasks]
download-models = "python tools/download-models.py"

[dependencies]
python = ">=3.12"
requests = ">=2.31.0"
